---
layout: inside
title: Print Design &amp; Illustration
---
<h1>Print design &amp; Illustration</h1>
<div class="grid-auto">
  <% data.print.shuffle.each do |image| %>
    <% if !image.auto %>
      <div class="box-img grid-auto-set-<%= image.position %>">
        <img src="<%= image.url %>" alt="<%= image.title %>" />
      </div>
    <%else%>
      <div class="box-img">
        <img src="<%= image.url %>" alt="<%= image.title %>" />
      </div>
    <% end %>

  <% end %>

</div>